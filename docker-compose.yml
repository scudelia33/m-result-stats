version: '3'
services:
  mstats-nginx:
    container_name: "mstats-nginx"
    build:
      context: ./docker/nginx
    depends_on:
      - mstats-app
    ports:
      - 8102:80
    volumes:
      - ./src:/src  # Laravel アプリを /src にマウント

  mstats-app:
    container_name: "mstats-app"
    build:
      context: ./docker/php
    depends_on:
      - mstats-mysql
    ports:
      - 5173:5173
    volumes:
      - ./src:/src
      - /src/node_modules
      - /src/vendor
      - ./docker/php/php.ini:/usr/local/etc/php/php.ini

  mstats-mysql:
    image: mariadb:latest  # 最新安定版を使用
    command: --max_allowed_packet=32505856
    container_name: "mstats-mysql"
    volumes:
      - ./docker/mysql:/var/lib/mysql
    environment:
      - MARIADB_ROOT_PASSWORD=root
      - MARIADB_DATABASE=mstats
      - TZ=Asia/Tokyo
    ports:
      - 13306:3306

  mstats-redis:
    image: redis:alpine
    container_name: "mstats-redis"
    ports:
      - 16379:6379
